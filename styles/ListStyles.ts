import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        width: '100%',
        height: '100%',
        backgroundColor: 'white',
    },
    item: {
        padding: 20,
        borderBottomWidth: 1,
    },
    title: {
        paddingTop: 10,
        paddingLeft: 10,
        fontSize: 20,
        fontWeight: 'bold'
    },
    text: {
        fontSize: 16,
    }
});
