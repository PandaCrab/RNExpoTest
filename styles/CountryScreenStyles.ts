import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        padding: 5,
        width: '100%',
        height: '100%',
        display: 'flex',
        backgroundColor: 'white', 
    },
    themedText: {
        fontSize: 18,
        fontWeight: 'bold',

    },
    text: {
        marginBottom: 10,
        fontSize: 16,
    }
});
