import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        width: '100%',
        height: '100%',
        backgroundColor: 'white',
    },
    item: {
        padding: 20,
        borderBottomWidth: 1,
    },
    text: {
        fontSize: 16,
        fontWeight: 'bold'
    }
});
